---
name: Adapter
connetions: "[[1. Структурные паттерны]]"
description: Выступает прослойкой, позволяющей объектам с несовместимыми интерфейсами работать вместе.
use_key: Интеграция сторонних библиотек, легаси-кода или внешних API без их изменения.
anti_use_key: Доступен исходный код обеих систем и они легко поддаются прямому рефакторингу.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Обертки над внешними компонентами (COMОбъект) или интеграция с REST API сторонних систем. Например, типовой модуль 1С ожидает вызов функции ОтправитьSMS(Телефон, Текст), но внешний HTTP API провайдера требует отправки JSON-структуры с полями recipient_number и message_body. Адаптер берет параметры 1С и конвертирует их в формат внешнего API.
---
```meta-bind-embed
[[embed_pattern_note]]
```