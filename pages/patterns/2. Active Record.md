---
name: Active Record
connetions: "[[1. Архитектурные паттерны]]"
description: Объект инкапсулирует данные таблицы БД, бизнес-логику и методы сохранения (сохраняет сам себя).
use_key: Прототипы, монолиты с простой CRUD-логикой без сложных связей таблиц.
anti_use_key: Enterprise-системы со сложным доменом (жесткое связывание бизнес-логики и СУБД).
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: "Ссылочные объекты, такие как СправочникОбъект.Номенклатура или ДокументОбъект.РеализацияТоваровУслуг. Сам объект содержит свои свойства (реквизиты) и сам же содержит методы для работы с базой данных: .Записать(), .Удалить(), .Прочитать(). Логика сохранения и бизнес-логика неразрывно связаны в одном модуле объекта."
---
```meta-bind-embed
[[embed_pattern_note]]
```