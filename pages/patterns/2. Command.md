---
name: Command
connetions: "[[1. Поведенческие паттерны]]"
description: Инкапсулирует вызов метода со всеми аргументами в отдельный объект-команду.
use_key: Системы очередей заданий (брокеры), отложенное выполнение (Cron), реализация отмены операций (Undo/Redo).
anti_use_key: Простые синхронные вызовы методов без необходимости вести историю или очередь.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Механизм фоновых и регламентных заданий. Вызов ФоновыеЗадания.Выполнить("ИмяМетода", МассивПараметров) упаковывает информацию о том, что нужно сделать, и аргументы в объект базы данных. Платформа ставит этот объект в очередь, а рабочий процесс сервера 1С забирает его и выполняет в изолированном контексте. Отправителю не нужно знать, когда и как именно выполнится код.
---
```meta-bind-embed
[[embed_pattern_note]]
```