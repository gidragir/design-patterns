---
name: Unit of Work
connetions: "[[1. Архитектурные паттерны]]"
description: Отслеживает изменения бизнес-объектов в памяти и координирует их запись в БД единой транзакцией.
use_key: Сложные бизнес-операции, затрагивающие несколько агрегатов, требующих ACID.
anti_use_key: Изолированные однотабличные CRUD-операции без сложных связей.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Явное использование транзакций платформы (НачатьТранзакцию(), ЗафиксироватьТранзакцию()). Когда проводится сложный документ (например, "Расчет себестоимости"), платформа или разработчик накапливают изменения. Только при успешном завершении всего сложного алгоритма данные физически фиксируются в СУБД; если где-то возникла ошибка (ОтменитьТранзакцию()), все изменения откатываются целиком, не оставляя "битых" движений.
---
```meta-bind-embed
[[embed_pattern_note]]
```