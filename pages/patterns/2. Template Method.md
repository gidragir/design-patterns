---
name: Template Method
connetions: "[[1. Поведенческие паттерны]]"
description: Определяет скелет алгоритма в суперклассе, делегируя реализацию конкретных шагов подклассам.
use_key: Жесткий каркас алгоритма (например, импорт/экспорт), где шаги могут меняться, но их порядок строго фиксирован.
anti_use_key: Плоские, простые алгоритмы без вариантов расширения логики.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: "Типовой механизм проведения документов в библиотеке стандартных подсистем (БСП) или конфигурациях ERP. В общем модуле описан жесткий каркас ОбработкаПроведения: 1) Очистить старые движения, 2) Подготовить данные (вызов процедуры объекта), 3) Выполнить проверки (вызов процедуры объекта), 4) Записать новые движения. Сам документ не меняет порядок этих шагов, он лишь реализует логику внутри конкретных экспортных процедур-перехватчиков (шагов алгоритма)."
---
```meta-bind-embed
[[embed_pattern_note]]
```