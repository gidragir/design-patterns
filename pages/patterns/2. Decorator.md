---
name: Decorator
connetions: "[[1. Структурные паттерны]]"
description: Динамически добавляет объекту новые обязанности, оборачивая его в класс-обертку.
use_key: Реализация сквозного функционала (логирование, кэширование) поверх разных классов без изменения их кода.
anti_use_key: Дополнительное поведение жестко привязано к конкретному типу и не меняется.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Использование механизма расширений конфигурации (аннотации &Перед, &После, &Вместо). Оригинальный модуль менеджера документа не меняется, но расширение "оборачивает" его стандартную процедуру проведения, динамически добавляя, например, запись в нетиповой регистр сведений или дополнительную проверку заполнения перед вызовом основного типового кода.
---
```meta-bind-embed
[[embed_pattern_note]]
```