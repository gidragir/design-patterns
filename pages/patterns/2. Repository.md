---
name: Repository
connections: "[[1. Архитектурные паттерны]]"
description: Создает абстракцию над механизмом хранения данных, предоставляя коллекциеподобный интерфейс.
use_key: Использование DDD, сложная доменная логика, необходимость мокирования БД для тестов.
anti_use_key: Простые приложения с жесткой привязкой к одному фреймворку (Active Record).
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Модули менеджеров объектов (Справочники.Номенклатура, Документы.ЗаказПокупателя). Бизнес-логика вызывает методы НайтиПоКоду(), СоздатьЭлемент(), Выбрать(), не зная, какие именно SQL-запросы платформа 1С отправляет в MS SQL Server или PostgreSQL. Менеджер скрывает детали физического хранения.
---
```meta-bind-embed
[[embed_pattern_note]]
```