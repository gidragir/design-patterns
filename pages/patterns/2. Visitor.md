---
name: Visitor
connetions: "[[1. Поведенческие паттерны]]"
description: Добавляет новые операции в иерархию классов объектов без изменения их исходного кода.
use_key: Сложные замороженные структуры объектов, требующие регулярного написания новых внешних алгоритмов обработки.
anti_use_key: Частое изменение самой структуры данных (добавление новых типов элементов потребует правки всех посетителей).
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Внешние обработки заполнения табличных частей или создания связанных объектов. Сами документы (Реализация, Поступление, Списание) ничего не знают о том, как их будут заполнять. Обработка (Посетитель) получает ссылку или объект документа извне, понимает его тип и применяет к нему свой внешний алгоритм, не требуя внесения изменений в модули объектов самих документов.
---
```meta-bind-embed
[[embed_pattern_note]]
```