---
name: State
connetions: "[[1. Поведенческие паттерны]]"
description: Позволяет объекту изменять свое поведение при смене внутреннего состояния (подобие конечного автомата).
use_key: Сложная логика бизнес-процессов (BPM), документы со сложным жизненным циклом и маршрутами.
anti_use_key: Объект имеет 1-2 простых статуса, не требующих ветвления логики.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Управление поведением документа в зависимости от его реквизита "Статус" (например, "Черновик", "Согласован", "Закрыт"). Вместо того чтобы в обработчике ПередЗаписью писать десятки условий проверок прав и допустимых действий для каждого статуса, логика каждого статуса выделяется в отдельный класс (или функцию-обработчик), которому контекст передает управление.
---
```meta-bind-embed
[[embed_pattern_note]]
```