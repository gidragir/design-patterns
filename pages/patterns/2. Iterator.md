---
name: Iterator
connetions: "[[1. Поведенческие паттерны]]"
description: Последовательный обход элементов коллекции без раскрытия ее внутренней структуры.
use_key: Сложные деревья, графы, необходимость нескольких вариантов обхода одной коллекции.
anti_use_key: Простые одномерные массивы, покрываемые стандартными циклами языка.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Объект Выборка из результата запроса (РезультатЗапроса.Выбрать()). Клиентский код пишет стандартный цикл Пока Выборка.Следующий() Цикл, совершенно не зная, как именно данные лежат в оперативной памяти сервера — в виде массива, курсора СУБД или сложного дерева группировок. Итератор скрывает эту сложность и предоставляет единый интерфейс .Следующий().
---
```meta-bind-embed
[[embed_pattern_note]]
```