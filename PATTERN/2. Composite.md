---
name: Composite
connetions: "[[1. Структурные паттерны]]"
description: Организует объекты в древовидные структуры, позволяя работать с листьями и ветками через единый интерфейс.
use_key: Иерархические структуры данных (деревья, файловые системы, оргструктуры, многоуровневые меню).
anti_use_key: Плоские списки и реляционные таблицы без вложенности.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: 'Иерархические справочники (например, "Номенклатура" или "Подразделения"). И группа (папка), и элемент справочника поддерживают одинаковые базовые действия: у них есть код, наименование, метод УстановитьПометкуУдаления(). Если нужно рассчитать общую сумму остатков по всей папке, алгоритм рекурсивно обходит вложенные папки и элементы, не различая их базовые интерфейсы.'
---
```meta-bind-embed
[[embed_pattern_note]]
```