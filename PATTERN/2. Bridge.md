---
name: Bridge
connetions: "[[1. Структурные паттерны]]"
description: Разделяет абстракцию (логику) и реализацию на две независимые иерархии.
use_key: "Необходимость расширять систему в двух независимых плоскостях (например: тип отчета и формат выгрузки)."
anti_use_key: Монолитная логика без вариативности в реализации.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Подсистема "Печатные формы" в БСП. Есть абстракция — сам отчет или печатная форма (например, "ТОРГ-12", "Счет на оплату"). И есть реализация формата вывода (Табличный документ 1С, PDF, Excel). Разработчику не нужно создавать отдельные объекты СчетНаОплатуPDF, СчетНаОплатуExcel, ТОРГ12PDF. Логика формирования данных (Абстракция) отделена от механизма рендеринга (Реализация).
---
```meta-bind-embed
[[embed_pattern_note]]
```