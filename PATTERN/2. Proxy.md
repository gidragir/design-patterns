---
name: Proxy
connetions: "[[1. Структурные паттерны]]"
description: Объект-заменитель, перехватывающий вызовы к реальному объекту для кэширования, проверок или ленивой загрузки.
use_key: Кэширование результатов БД, защита эндпоинтов, отложенная инициализация тяжелых ресурсов.
anti_use_key: Оригинальный класс легковесный, локальный и не требует контроля доступа.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Общие модули с установленным свойством "Повторное использование возвращаемых значений" (кэширование на время сезова/сеанса). Клиентский код всегда вызывает функцию КэширующийМодуль.ПолучитьЦену(Номенклатура), не задумываясь о том, будет ли выполнен реальный SQL-запрос к базе данных или платформа отдаст ранее сохраненный результат из оперативной памяти. Модуль выступает прокси-сервером для базы данных.
---
```meta-bind-embed
[[embed_pattern_note]]
```