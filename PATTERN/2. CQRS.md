---
name: CQRS
connections: "[[1. Архитектурные паттерны]]"
description: Строго разделяет модели изменения состояния системы (Команды) и модели чтения данных (Запросы).
use_key: Высоконагруженные системы, микросервисы со сложной логикой домена и явной асимметрией нагрузки.
anti_use_key: Простые CRUD-приложения (избыточная сложность и дублирование).
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Разделение проведения документов и построения отчетов. Проведение документа (Command) выполняет сложную логику проверок и делает записи в физические таблицы регистров. Построение отчета (Query) никогда не читает структуру самого документа, а обращается к оптимизированным виртуальным таблицам регистров (Остатки, Обороты), которые специально подготовлены платформой для быстрого чтения агрегированных данных.
---
```meta-bind-embed
[[embed_pattern_note]]
```