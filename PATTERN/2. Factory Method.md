---
name: Factory Method
connections: "[[1. Порождающие паттерны]]"
description: Определяет общий интерфейс создания объекта, позволяя подклассам изменить тип создаваемого экземпляра.
use_key: Заранее неизвестны типы зависимостей, необходимость делегировать создание объектов подклассам.
anti_use_key: Класс продукта один и расширение системы не планируется.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Общие модули с серверными функциями-фабриками. Например, функция ПолучитьОбработчикПечати(ТипДокумента), которая внутри через конструкцию Если ... ИначеЕсли возвращает экспортные методы разных обработок (для Заказа, для Счета, для Накладной). Вызывающий код получает абстрактный объект и просто вызывает у него экспортную процедуру СформироватьПечатнуюФорму(), не зная точного типа обработки.
---
```meta-bind-embed
[[embed_pattern_note]]
```