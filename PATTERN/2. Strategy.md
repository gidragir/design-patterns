---
name: Strategy
connetions: "[[1. Поведенческие паттерны]]"
description: Инкапсулирует семейство алгоритмов в отдельные классы, делая их взаимозаменяемыми.
use_key: Множество родственных поведений, отличающихся только алгоритмом; избавление от массивных if/switch.
anti_use_key: Алгоритм имеет пару простых вариантов, которые никогда не меняются.
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Подключаемые внешние обработки для расчета скидок или заполнения табличных частей. Документ "Реализация товаров" не содержит жестко запрограммированной логики новогодней акции. Он получает ссылку на обработку, вызывает у нее стандартизированную экспортную процедуру РассчитатьСкидку(ТабличнаяЧасть), и обработка выполняет свой алгоритм.
---
```meta-bind-embed
[[embed_pattern_note]]
```