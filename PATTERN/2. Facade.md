---
name: Facade
connetions: "[[1. Структурные паттерны]]"
description: Предоставляет простой унифицированный интерфейс к сложной подсистеме из множества классов.
use_key: Интеграция со сложными API, рефакторинг монолита (выделение слоев).
anti_use_key: Подсистема состоит из 1-2 простых классов (фасад не добавляет ценности).
obsidianUIMode: preview
type: pattern_note
cssclasses:
  - hide-properties_reading
analogy: Экспортная процедура в серверном общем модуле, например, РозничныеПродажи.ОформитьПродажу(ДанныеЧека). Внутри этой процедуры создается документ "ЧекККМ", пробивается фискальный чек через компоненту торгового оборудования, списываются бонусы с карты лояльности и отправляется SMS клиенту. Клиентский код (например, обработчик нажатия кнопки на форме) вызывает только одну процедуру фасада, ничего не зная об устройстве этих подсистем.
---
```meta-bind-embed
[[embed_pattern_note]]
```